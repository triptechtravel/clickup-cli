---
title: "clickup task time running"
description: "Auto-generated reference for clickup task time running"
---

Show the current running timer

### Synopsis

Display the currently running time entry timer, if any.

```
clickup task time running [flags]
```

### Examples

```
  # Check running timer
  clickup task time running
```

### Options

```
  -h, --help              help for running
      --jq string         Filter JSON output using a jq expression
      --json              Output JSON
  -r, --raw               Output raw strings instead of JSON-encoded (use with --jq)
      --template string   Format JSON output using a Go template
```

### SEE ALSO

* [clickup task time](/clickup-cli/reference/clickup_task_time/)	 - Track time on ClickUp tasks

